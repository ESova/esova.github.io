```js
@@ -38,6 +38,7 @@ function PromocodeComponent (db, engine) { // SIGNUP COMPONENT - OVERLAY
     message: 'hide',
     // message: 'wrong promocode',
     // message: 'unmet requirements',
+    // message: 'not ready',
     promocode: ''
   }

@@ -112,12 +113,14 @@ function PromocodeComponent (db, engine) { // SIGNUP COMPONENT - OVERLAY
   /*****************************/
   // RENDER COMPONENT
   function renderPromocode () {
-    // @TODO @NOTE: when at lesson 4 end for signin => set currentLesson to 5
-    // => notifiy to finish first 4 lessons first
-    // => ask promo
-
     var vtree = STATUS.message === 'hide' ?
-      '' // renders nothing to hide the overlay
+      'hidden' // renders nothing to hide the overlay
+    : STATUS.message === 'ok' ?
+      'ok'
+    : STATUS.message === 'wrong promocode' ?
+      'wrong promocode'
+    : STATUS.message === 'unmet requirements' ?
+      'unmet requirements'
     : h('div', { className: x('promocode') }, [
       h('div', { className: x('welcome') }, [
         h('img', { src: header, className: x('header-picture')}),
@@ -158,24 +161,44 @@ function PromocodeComponent (db, engine) { // SIGNUP COMPONENT - OVERLAY
         ])
       ])
     ])
-    return vtree
+    // PUSH UPDATE
+    engine.push({
+      type: 'vtree',
+      key: 'promocode',
+      value: vtree
+    })
   }


   /*****************************/
   // INCOMING COMMUNICATION CHANNELS
+
+
+  // @TODO @NOTE: when at lesson 4 end for signin => set currentLesson to 5
+  // => notifiy to finish first 4 lessons first
+  // => ask promo
+
+  // var vtree = STATUS.message === 'hide' ?
         ])
       ])
     ])
-    return vtree
+    // PUSH UPDATE
+    engine.push({
+      type: 'vtree',
+      key: 'promocode',
+      value: vtree
+    })
   }


   /*****************************/
   // INCOMING COMMUNICATION CHANNELS
+
+
+  // @TODO @NOTE: when at lesson 4 end for signin => set currentLesson to 5
+  // => notifiy to finish first 4 lessons first
+  // => ask promo
+
+  // var vtree = STATUS.message === 'hide' ?
+  //   'hidden' // renders nothing to hide the overlay
+  // : STATUS.message === 'ok' ?
+  //   'ok'
+  // : STATUS.message === 'wrong promocode' ?
+  //   'wrong promocode'
+  // : STATUS.message === 'unmet requirements' ?
+  //   'unmet requirements'
+  // : h('div', { className: x('promocode') }, [
+  //
+
   db.on('put', function (key, value) {
     value = defined(value, {})
     if (key === 'OVERLAY') {
       value = JSON.parse(value)
       db.get('esova-currentLesson', function (error, lastlesson) {
+        debugger
         if (value.status === 'no valid promocode') {
           var deny = lastlesson ? lastlesson <= FREE_LESSONS : false
-          if (!deny) engine.push({ // AUTH FAIL + currentLesson <= FREE_LESSONS
-            type: 'vtree',
-            key: 'promocode',
-            value: render('allow')
+  //   'hidden' // renders nothing to hide the overlay
+  // : STATUS.message === 'ok' ?
+  //   'ok'
+  // : STATUS.message === 'wrong promocode' ?
+  //   'wrong promocode'
+  // : STATUS.message === 'unmet requirements' ?
+  //   'unmet requirements'
+  // : h('div', { className: x('promocode') }, [
+  //
+
   db.on('put', function (key, value) {
     value = defined(value, {})
     if (key === 'OVERLAY') {
       value = JSON.parse(value)
       db.get('esova-currentLesson', function (error, lastlesson) {
+        debugger
         if (value.status === 'no valid promocode') {
           var deny = lastlesson ? lastlesson <= FREE_LESSONS : false
-          if (!deny) engine.push({ // AUTH FAIL + currentLesson <= FREE_LESSONS
-            type: 'vtree',
-            key: 'promocode',
-            value: render('allow')
-          })
+          if (!deny) STATUS.message = 'hide' // AUTH FAIL + currentLesson <= FREE_LESSONS
+
           else if (value.status === 'allow invalid') engine.push({
             type: 'vtree',
             key: 'promocode',

```
